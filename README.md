This is a sample project to demonstrate using Iron meteor to quickly create meteor applications using scaffolding.

To run app do following steps:

- ```npm install -g iron-meteor```
- ```iron add aldeed:simple-schema```
- ```iron run```